server:
  port: ${PORT:8000}

spring:
  profiles:
    active: template
  webflux:
    static-path-pattern: /h5/**
  web:
    resources:
      static-locations: classpath:/static/

app:
  deepseek:
    api-key: sk-23afa0615c24426283f0da1583d246bd
    base-url: ${DEEPSEEK_BASE_URL:https://api.deepseek.com}
  model:
    name: ${MODEL_NAME:deepseek-v4-flash}
    coordinator: ${COORDINATOR_MODEL:deepseek-v4-flash}
    follow-up: ${FOLLOW_UP_MODEL:deepseek-v4-flash}
  jwt:
    secret: ${JWT_SECRET:}
    expire-hours: 24
  data:
    dir: data
  upload:
    dir: static/uploads/account_opening

logging:
  level:
    com.cropagent: DEBUG
